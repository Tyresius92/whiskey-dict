export const BREAKPOINTS = {
  xsMax: 576,
  sMax: 768,
  mMax: 992,
  lMax: 1200,
};

export const QUERIES = {
  screenSizeXs: `(max-width: ${BREAKPOINTS.xsMax}px)`,
  screenSizeS: `(max-width: ${BREAKPOINTS.sMax}px)`,
  screenSizeM: `(max-width: ${BREAKPOINTS.mMax}px)`,
  screenSizeL: `(max-width: ${BREAKPOINTS.lMax}px)`,
};
